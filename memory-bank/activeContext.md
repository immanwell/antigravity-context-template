# 📌 Active Session Context

## 1. Current Session Goal
[PLACEHOLDER] E.g., Integrate the WebSocket library and define the initial connection endpoint.

## 2. Recent Accomplishments (What was just completed)
* Initial project scaffolding with `npm init`.
* Defined base folder structure (src/, components/).

## 3. Active Decisions / Open Questions
* Decision: We are using Redis for session management.
* Question: Should message history be persisted in PostgreSQL or MongoDB?
